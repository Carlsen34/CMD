programa test;

var a,b: inteiro;


funcao soma: inteiro;
var c,a: inteiro;

  procedimento ler;
  inicio
     leia (c);
     leia (a);
     escreva (soma);
fim;

  procedimento loop;
  var x: inteiro;
  
  funcao f2:inteiro;
  inicio
  
  escreva(a);
  f2:=10;
  
  fim;
  
  
  inicio
    leia (x);   {para parar digite um valor maior que 9 }
    se x < f2
    entao ler;
  fim;

 inicio
  loop;
  soma:= c+a;
 fim;

inicio
  escreva(soma)
fim.
